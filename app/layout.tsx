import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import Header from '@/components/Header'
import Footer from '@/components/Footer'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'EverPlayToys - Where Fun Never Ends',
  description: 'Discover the best toys for kids of all ages. From educational toys to outdoor play equipment, we have everything to make childhood magical.',
  keywords: 'toys, kids toys, educational toys, outdoor toys, action figures, dolls, games',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Header />
        <main className="min-h-screen">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  )
}
